{
  "version": 2,
  "buildCommand": "cd resume-builder && npm install && npx prisma generate",
  "outputDirectory": "resume-builder",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
